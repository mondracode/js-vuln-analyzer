app.use(cors());

eval(req.attribute)

if (null == undefined) {
    console.log("hmmm")
} else if (null != undefined) {
    console.log("That second condition is not necessary")
}

const protoPollution = {
    "__proto__": {
        "toString": "broken parameter"
    }
}

window.location.href = redirectUrl;

var weakRandomNumber = Math.random();

var username = "admin";
let api_key = "pass";

let password = env.some.secure.credential

app.get("/", (req, res) => {
    const name = req.query.name;
    res.send(`<h1>Hello, ${name}</h1>`);
});

app.get("/home", (req, res) => {
  // Do a resource-intensive operation
  while(true) {}
});

var user_input = "console.log('This is user input.');";
setTimeout(user_input, 1000);

var encryptedData = CryptoJS.DES.encrypt("data", "key");
console.log(encryptedData);

var userInputHash = md5(userInput);
if (userInputHash == "5f4dcc3b5aa765d61d8327deb882cf99") {
    console.log("Inseguro: comparación de hash insegura");
}
