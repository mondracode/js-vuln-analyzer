app.use(cors());

// Eval call
eval(req.attribute)
eval('setTimeout(9999999)')

if (null == undefined) {
    console.log("hmmm")
}

const protoPollution = {
    "__proto__": {
        "toString": "broken parameter"
    }
}

window.location.href = redirectUrl;

var weakRandomNumber = Math.random();
var username = "admin";
const password = "password123";